port: "8080"
data_dir: "data"
read_timeout: "10s"
write_timeout: "30s"
idle_timeout: "60s"
num_workers: 8

convert:
  data_dir: "data"
  out_file: "data/records.json"
  pattern: "data/File *"

search:
  default_limit: 20
  max_limit: 100
  default_offset: 0